server {
	listen 80;
	server_name _; # Accetta qualsiasi dominio/IP

	# Directory root per file statici
	root /var/www/marcatempo/public;
	index index.html;

	# Log
	access_log /var/log/nginx/marcatempo_access.log;
	error_log /var/log/nginx/marcatempo_error.log;

	# Serve il frontend
	location / {
		try_files $uri $uri/ /index.html;
	}

	# Proxy per API Node.js
	location /api/ {
		proxy_pass http://localhost:3000/api/;
		proxy_http_version 1.1;


	# Headers
		proxy_set_header Upgrade $http_upgrade;
		proxy_set_header Connection 'upgrade';
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;

		# Timeout
		proxy_connect_timeout 60s;
		proxy_send_timeout 60s;
		proxy_read_timeout 60s;
		proxy_cache_bypass $http_upgrade;
	} 
}
